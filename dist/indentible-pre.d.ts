/**
 * Strip a uniform amount of space off the front of each line and return the lines separated
 * in an array.
 */
export declare function indentablePreToLines(rawText: string, tabWidth?: number): string[];
